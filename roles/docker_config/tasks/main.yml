---
- name: Write docker default conifg
  template:
    src: docker_default.j2
    dest: /etc/default/docker


- name: Restart docker
  service:
    name: docker
    state: restarted
    enabled: yes
